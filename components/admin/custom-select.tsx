"use client";
import { ChevronDown,Check } from "lucide-react";
import { useEffect,useRef,useState } from "react";

type Option={value:string;label:string};
export function CustomSelect({value,onChange,options,placeholder="Select option",className=""}:{value:string;onChange:(value:string)=>void;options:Option[];placeholder?:string;className?:string}){const[open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{const close=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node))setOpen(false)};document.addEventListener("mousedown",close);return()=>document.removeEventListener("mousedown",close)},[]);const selected=options.find(o=>o.value===value);return <div ref={ref} className={`relative ${className}`}><button type="button" onClick={()=>setOpen(v=>!v)} className="flex h-11 w-full items-center justify-between rounded-xl border border-[#143124]/12 bg-white px-3 text-left text-sm outline-none transition focus:border-[#0b6b3a]"><span className={selected?"text-[#143124]":"text-[#8a948c]"}>{selected?.label||placeholder}</span><ChevronDown className={`size-4 transition ${open?"rotate-180":""}`}/></button>{open&&<div className="absolute z-50 mt-2 max-h-64 w-full overflow-auto rounded-xl border border-[#143124]/10 bg-white p-1 shadow-xl">{options.map(o=><button type="button" key={o.value} onClick={()=>{onChange(o.value);setOpen(false)}} className={`flex w-full items-center justify-between rounded-lg px-3 py-2.5 text-left text-sm hover:bg-[#f2f7ed] ${o.value===value?"bg-[#eef5df] font-semibold text-[#0b6b3a]":""}`}><span>{o.label}</span>{o.value===value&&<Check className="size-4"/>}</button>)}</div>}</div>}
