"use client";

import Image from "next/image";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
import { Edit3, Eye, Plus, Search, Star, Trash2 } from "lucide-react";
import { AdminSelect } from "@/components/admin/admin-select";
import { AppDialog } from "@/components/ui/app-dialog";

type PackageItem = { _id:string; title:string; slug:string; category:string; location:string; startingPrice:number; durationDays:number; durationNights:number; status:"draft"|"published"; featured:boolean; heroImage?:{url?:string}; updatedAt:string };
const statusOptions=[{value:"all",label:"All statuses"},{value:"published",label:"Published"},{value:"draft",label:"Draft"}];
export function TourPackageManager() {const [items,setItems]=useState<PackageItem[]>([]);const [search,setSearch]=useState("");const [status,setStatus]=useState("all");const [loading,setLoading]=useState(true);const [error,setError]=useState("");const[pendingDelete,setPendingDelete]=useState<PackageItem|null>(null);const[dialogError,setDialogError]=useState("");
  useEffect(()=>{let cancelled=false;const timer=window.setTimeout(async()=>{setLoading(true);setError("");const params=new URLSearchParams({limit:"50",status});if(search.trim())params.set("search",search.trim());try{const res=await fetch(`/api/admin/tour-packages?${params.toString()}`,{cache:"no-store"});const json=await res.json();if(!res.ok)throw new Error(json.error||"Unable to load tour packages.");if(!cancelled)setItems(json.items||[])}catch(err){if(!cancelled)setError(err instanceof Error?err.message:"Unable to load tour packages.")}finally{if(!cancelled)setLoading(false)}},250);return()=>{cancelled=true;window.clearTimeout(timer)}},[search,status]);
  async function remove(){if(!pendingDelete)return;const item=pendingDelete;setPendingDelete(null);try{const res=await fetch(`/api/admin/tour-packages/${item._id}`,{method:"DELETE"});const json=await res.json();if(!res.ok)throw new Error(json.error||"Unable to delete package.");setItems(current=>current.filter(entry=>entry._id!==item._id))}catch(e){setDialogError(e instanceof Error?e.message:"Unable to delete package.")}}
  const counts=useMemo(()=>({total:items.length,published:items.filter(x=>x.status==="published").length,drafts:items.filter(x=>x.status==="draft").length}),[items]);
  return <div className="space-y-6"><div className="flex flex-col justify-between gap-4 xl:flex-row xl:items-end"><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[#68806f]">Content Management</p><h1 className="mt-1 text-3xl font-black">Tour Packages</h1><p className="mt-2 max-w-2xl text-sm text-[#68806f]">Manage package content used by the tour listing cards and dynamic package-detail pages.</p></div><Link href="/admin/tour-packages/new" className="inline-flex items-center justify-center gap-2 rounded-xl bg-[#0b6b3a] px-5 py-3 text-sm font-bold text-white shadow-sm hover:bg-[#07572f]"><Plus className="size-4"/>Add Tour Package</Link></div><div className="grid gap-3 sm:grid-cols-3">{[["Total packages",counts.total],["Published",counts.published],["Drafts",counts.drafts]].map(([name,value])=><div key={String(name)} className="rounded-2xl border border-[#143124]/10 bg-white p-5"><p className="text-sm text-[#68806f]">{name}</p><p className="mt-1 text-3xl font-black">{value}</p></div>)}</div><div className="rounded-2xl border border-[#143124]/10 bg-white p-4 shadow-sm"><div className="grid gap-3 md:grid-cols-[1fr_210px]"><label className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-[#68806f]"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search packages, locations or categories…" className="w-full rounded-xl border border-[#143124]/15 bg-[#f8faf6] py-3 pl-10 pr-4 text-sm outline-none focus:border-[#0b6b3a]"/></label><AdminSelect value={status} onChange={setStatus} options={statusOptions}/></div></div>{error&&<div className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}{loading?<div className="rounded-2xl border border-[#143124]/10 bg-white p-10 text-center text-sm text-[#68806f]">Loading tour packages…</div>:items.length===0?<div className="rounded-2xl border border-dashed border-[#143124]/20 bg-white p-12 text-center"><h2 className="font-bold">No tour packages found</h2><p className="mt-2 text-sm text-[#68806f]">Create your first package or adjust the current filters.</p></div>:<div className="overflow-hidden rounded-2xl border border-[#143124]/10 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[920px] text-left text-sm"><thead className="bg-[#f6f8f4] text-xs uppercase tracking-wide text-[#68806f]"><tr><th className="px-5 py-4">Package</th><th className="px-5 py-4">Category</th><th className="px-5 py-4">Duration</th><th className="px-5 py-4">Price</th><th className="px-5 py-4">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{items.map(item=><tr key={item._id} className="border-t border-[#143124]/8 hover:bg-[#fbfcfa]"><td className="px-5 py-4"><div className="flex items-center gap-3"><div className="relative size-14 overflow-hidden rounded-xl bg-[#eef3ea]">{item.heroImage?.url?<Image src={item.heroImage.url} alt="" fill unoptimized className="object-cover"/>:null}</div><div><div className="flex items-center gap-2"><p className="font-bold">{item.title}</p>{item.featured&&<Star className="size-4 fill-amber-400 text-amber-400"/>}</div><p className="mt-1 text-xs text-[#68806f]">{item.location} · /{item.slug}</p></div></div></td><td className="px-5 py-4">{item.category}</td><td className="px-5 py-4">{item.durationDays}D / {item.durationNights}N</td><td className="px-5 py-4 font-semibold">₹{Number(item.startingPrice||0).toLocaleString("en-IN")}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${item.status==="published"?"bg-emerald-100 text-emerald-700":"bg-amber-100 text-amber-700"}`}>{item.status}</span></td><td className="px-5 py-4"><div className="flex justify-end gap-2">{item.status==="published"&&<a href={`/tour-packages/${item.slug}`} target="_blank" rel="noreferrer" className="rounded-lg border border-[#143124]/10 p-2 hover:bg-[#f3f6f1]"><Eye className="size-4"/></a>}<Link href={`/admin/tour-packages/${item._id}`} className="rounded-lg border border-[#143124]/10 p-2 hover:bg-[#f3f6f1]"><Edit3 className="size-4"/></Link><button onClick={()=>setPendingDelete(item)} className="rounded-lg border border-red-200 p-2 text-red-600 hover:bg-red-50"><Trash2 className="size-4"/></button></div></td></tr>)}</tbody></table></div></div>}<AppDialog open={Boolean(pendingDelete)} title="Delete tour package?" message={pendingDelete?`Delete “${pendingDelete.title}”? This cannot be undone.`:""} tone="danger" confirmLabel="Delete Package" onConfirm={remove} onCancel={()=>setPendingDelete(null)}/><AppDialog open={Boolean(dialogError)} title="Action failed" message={dialogError} tone="danger" onConfirm={()=>setDialogError("")}/></div>;
}
