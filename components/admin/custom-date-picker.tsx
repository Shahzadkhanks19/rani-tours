"use client";
import { CalendarDays,ChevronLeft,ChevronRight } from "lucide-react";
import { useMemo,useState } from "react";

const pad=(n:number)=>String(n).padStart(2,"0");
const toValue=(d:Date)=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`;
export function CustomDatePicker({value,onChange,placeholder="Select date"}:{value:string;onChange:(value:string)=>void;placeholder?:string}){const selected=value?new Date(`${value}T00:00:00`):null;const[open,setOpen]=useState(false);const[month,setMonth]=useState(()=>selected&&!Number.isNaN(selected.getTime())?new Date(selected.getFullYear(),selected.getMonth(),1):new Date(new Date().getFullYear(),new Date().getMonth(),1));const days=useMemo(()=>{const first=new Date(month.getFullYear(),month.getMonth(),1);const start=new Date(first);start.setDate(1-first.getDay());return Array.from({length:42},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);return d})},[month]);const label=selected&&!Number.isNaN(selected.getTime())?selected.toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}):placeholder;return <div className="relative"><button type="button" onClick={()=>setOpen(v=>!v)} className="flex h-11 w-full items-center justify-between rounded-xl border border-[#143124]/12 bg-white px-3 text-left text-sm"><span className={value?"":"text-[#8a948c]"}>{label}</span><CalendarDays className="size-4 text-[#0b6b3a]"/></button>{open&&<div className="absolute z-50 mt-2 w-[300px] max-w-[90vw] rounded-2xl border border-[#143124]/10 bg-white p-3 shadow-2xl"><div className="flex items-center justify-between"><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()-1,1))} className="rounded-lg p-2 hover:bg-[#f2f7ed]"><ChevronLeft className="size-4"/></button><strong className="text-sm">{month.toLocaleDateString("en-IN",{month:"long",year:"numeric"})}</strong><button type="button" onClick={()=>setMonth(new Date(month.getFullYear(),month.getMonth()+1,1))} className="rounded-lg p-2 hover:bg-[#f2f7ed]"><ChevronRight className="size-4"/></button></div><div className="mt-3 grid grid-cols-7 text-center text-[10px] font-bold uppercase text-[#7f8a81]">{["Su","Mo","Tu","We","Th","Fr","Sa"].map(d=><span key={d}>{d}</span>)}</div><div className="mt-1 grid grid-cols-7 gap-1">{days.map(d=>{const current=d.getMonth()===month.getMonth();const v=toValue(d);const active=v===value;return <button type="button" key={v} onClick={()=>{onChange(v);setOpen(false)}} className={`grid aspect-square place-items-center rounded-lg text-xs ${active?"bg-[#0b6b3a] font-bold text-white":current?"hover:bg-[#eef5df]":"text-[#b1b8b2] hover:bg-[#f7f8f6]"}`}>{d.getDate()}</button>})}</div></div>}</div>}
